import { SafeAreaView } from 'react-native-safe-area-context';
import { Text, View } from 'react-native';

export default function BoletosScreen() {
    return (
        <SafeAreaView>
            <View>
                <Text>Estás en Boletos</Text>
            </View>
        </SafeAreaView>

    );
}