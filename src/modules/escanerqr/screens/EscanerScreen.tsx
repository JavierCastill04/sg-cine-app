import { SafeAreaView } from 'react-native-safe-area-context';
import { Text, View } from 'react-native';

export default function EscanerScreen() {
    return (
        <SafeAreaView>
            <View>
                <Text>Estás en Escáner QR</Text>
            </View>
        </SafeAreaView>

    );
}