import { SafeAreaView } from 'react-native-safe-area-context';
import { Text, View } from 'react-native';

export default function ReservaScreen() {
    return (
        <SafeAreaView>
            <View>
                <Text>Estás en Reservas</Text>
            </View>
        </SafeAreaView>

    );
}